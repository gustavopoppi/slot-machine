package br.com.mvc.cacaniquel.controller;

public class CreditController {

    /*public void bet(double betValue, int multiplier) {
        if (this.getCreditValue() < betValue)
            throw  new RuntimeException("You don't have enough credit.");

        updateCredit(BET, betValue);

        SlotMachine slotMachine = new SlotMachine();
        slotMachine.newGame();
    }

    private void updateCredit(Bet bet, double value) {
        if (bet.equals(BET))
            this.creditValue -= value;

        if (bet.equals(DEPOSIT))
            this.creditValue += value;
    }

    public void insertCredit(double value) {
        this.creditValue += value;
    }*/

    //TODO GUSTAVO insertCredit
}
